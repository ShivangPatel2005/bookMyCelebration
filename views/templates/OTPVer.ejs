<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>

    <style>
        #webSiteLogo {
            display: block;
            margin: 50px auto 0px auto;
        }
        
        #OTPVerContainer {
            width: 30%;
            margin: 50px auto 100px auto;
            padding: 40px 40px 60px 40px;
            border-width: 2px;
            border-color: #ada1ff;
            border-style: solid;
            border-radius: 10px;
            box-shadow: 4px 4px #ada1ff;
        }
        
        #OTPHead {
            font-family: 'Belanosima', sans-serif;
            font-weight: 800;
            letter-spacing: 1.4px;
            text-align: center;
            margin-bottom: 40px;
        }
        
        #NavBar {
            background-color: #ada1ff;
        }
    </style>
</head>

<body>
    <%- include('header') %>
        <img src="webSiteLogo.png" alt="WebSite Logo" height="60px" id="webSiteLogo">
        <section id="OTPVerContainer">
            <h2 id="OtpHead">Verifiy OTP</h2>
            <div class="alert alert-primary" role="alert">OTP is sended to
                <%= mail %>
            </div>
            <div class="alert alert-primary" role="alert">Enter OTP below and verify your email account.</div>
            <form action="/OTPVer" method="post">
                <div class="form-floating mb-4">
                    <input type="password" class="form-control" id="OTPVerification" placeholder="OTP" name="OTPVerification" inputmode="numeric">
                    <label for="OTPVerification">Enter OTP</label>
                </div>
                <div class="d-grid">
                    <button type="submit" class="btn btn-outline-primary" style="font-weight: 700;">Submit</button>
                </div>
            </form>
        </section>
        <%- include('footer') %>
</body>

</html>